{
  "name": "i18n-code-completion",
  "displayName": "i18n-code-completion",
  "description": "i18n code completion",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.73.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "i18n-code-completion.helloWorld",
        "title": "Hello World"
      }
    ],
    "configuration": {
      "title": "JSON Code completion",
      "properties": {
        "jsonCodeCompletion.config": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "sourcePath": {
                "type": "string",
                "description": "Path of the source JSON file. It can be either absolute or relative to workspace root directory."
              },
              "destinationPattern": {
                "type": "string",
                "description": "Glob pattern of files to provide the code completion in (only .ts, .js and .html are supported)."
              }
            }
          },
          "default": "",
          "description": "Configuration of source files and destination files for code completion."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "dependencies": {
    "json-source-map": "^0.6.1",
    "minimatch": "^9.0.0"
  },
  "devDependencies": {
    "@types/glob": "8.0.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "16.x",
    "@types/vscode": "^1.73.0",
    "@typescript-eslint/eslint-plugin": "^5.42.0",
    "@typescript-eslint/parser": "^5.42.0",
    "@vscode/test-electron": "^2.2.0",
    "eslint": "^8.26.0",
    "glob": "10.2.2",
    "mocha": "^10.1.0",
    "typescript": "^4.8.4"
  }
}
